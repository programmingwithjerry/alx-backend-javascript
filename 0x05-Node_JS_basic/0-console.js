#!/usr/bin/env node
/**
 * This script defines a function `displayMessage` that prints a given string
 * argument to standard output (STDOUT).
 */

function displayMessage(message) {
    console.log(message);
}

// Example usage
   displayMessage('Hello, World!');
